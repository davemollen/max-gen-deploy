#!/bin/bash

cd $(dirname $0)

###################################################################################################

if (ls | grep -q ".tar.gz"); then
  cat *.tar.gz | base64 | curl -F 'package=@-' http://192.168.51.1/sdk/install
  echo
else
  echo "Run ./mod-build.sh first"
fi

###################################################################################################
